# Restaurant Website
Flask-based web app with simple HTML and CSS styling to view a list of restaurants, create, edit, and delete restaurants and the menu inside each restaurant.

## Usage
- Run `lotsofmenu.py` to generate the restaurants and menus to the database.
- Run `finalproject.py`
- Open browser and go to ***http://localhost:5000/restaurants***
